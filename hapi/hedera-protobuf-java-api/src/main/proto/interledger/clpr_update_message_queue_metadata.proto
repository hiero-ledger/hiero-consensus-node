/**
 * # Update CLPR message queue metadata
 *
 * ### Keywords
 * The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT",
 * "SHOULD", "SHOULD NOT", "RECOMMENDED", "MAY", and "OPTIONAL" in this
 * document are to be interpreted as described in
 * [RFC2119](https://www.ietf.org/rfc/rfc2119) and clarified in
 * [RFC8174](https://www.ietf.org/rfc/rfc8174).
 */
syntax = "proto3";

package org.hiero.hapi.interledger.clpr;

// SPDX-License-Identifier: Apache-2.0
option java_package = "org.hiero.hapi.interledger.clpr.protoc";
// <<<pbj.java_package = "org.hiero.hapi.interledger.clpr">>> This comment is special code for setting PBJ Compiler java package
option java_multiple_files = true;

import "block/stream/state_proof.proto";
import "interledger/state/clpr/clpr_ledger.proto";


/**
 * Update CLPR message queue metadata.
 */
message ClprUpdateMessageQueueMetadataTransactionBody {
  /**
   * The identifier of the remote ledger for which the message queue metadata is being updated.
   */
  org.hiero.hapi.interledger.state.clpr.ClprLedgerId ledger_id = 1;

  /**
   * State proof containing the message queue metadata encoded as a state item
   * leaf. Clients MUST verify the proof to ensure the authenticity of the
   * metadata before updating the local state.
   */
  com.hedera.hapi.block.stream.StateProof message_queue_metadata_proof = 2;
}
