/**
 * # Get CLPR Message Queue Metadata
 *
 * ### Keywords
 * The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT",
 * "SHOULD", "SHOULD NOT", "RECOMMENDED", "MAY", and "OPTIONAL" in this
 * document are to be interpreted as described in
 * [RFC2119](https://www.ietf.org/rfc/rfc2119) and clarified in
 * [RFC8174](https://www.ietf.org/rfc/rfc8174).
 */
syntax = "proto3";

package org.hiero.hapi.interledger.clpr;

// SPDX-License-Identifier: Apache-2.0
option java_package = "org.hiero.hapi.interledger.clpr.protoc";
// <<<pbj.java_package = "org.hiero.hapi.interledger.clpr">>> This comment is special code for setting PBJ Compiler java package
option java_multiple_files = true;

import "services/basic_types.proto";
import "services/query_header.proto";
import "services/response_header.proto";
import "interledger/state/clpr/clpr_ledger.proto";
import "block/stream/state_proof.proto";

/**
 * Query for the CLPR message queue metadata matching the given ledger id.
 */
message ClprGetMessageQueueMetadataQuery {
  /**
   * Standard information sent with every query operation.<br/>
   * This includes the signed payment and what kind of response is requested
   * (cost, state proof, both, or neither).
   */
  proto.QueryHeader header = 1;
  /**
   * The ledger id of the CLPR message queue metadata desired.
   */
  org.hiero.hapi.interledger.state.clpr.ClprLedgerId ledger_id = 2;
}

/**
 * A response to a `ClprGetMessageQueueMetadataQuery`.
 * <p>
 * On success, this MUST return a state proof of the `ClprMessageQueueMetadata` for the
 * ledger id provided in the query.
 */
message ClprGetMessageQueueMetadataResponse {
  /**
   * The standard response information for queries.
   * <p>
   * This includes the values requested in the `QueryHeader`
   * (cost, state proof, both, or neither).
   */
  proto.ResponseHeader header = 1;

  /**
   * The state proof containing the message queue metadata requested by the query.
   * <p>
   * This state proof contains the serialized ClprMessageQueueMetadata as the leaf payload.
   */
  com.hedera.hapi.block.stream.StateProof message_queue_metadata_proof = 2;
}
