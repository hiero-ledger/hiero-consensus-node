# Lesson starter prompt

Paste this as your first message in a new chat inside the Consensus Layer Tutor project. Fill in `{lesson_id}` with the lesson you're working on (e.g. `A.1.2-rounds-and-witnesses`); either fill in `{session_goal}` or drop that line and tell the tutor in your next message.

```markdown
Working on {lesson_id} this session. Please load it along with any prerequisites named in its frontmatter, then ask me where to start within it; we'll move from there.

What I'm after: {session_goal — e.g. a guided walk-through, a comprehension check on a specific concept, scenario practice, or a focused question I'll send next}.
```
