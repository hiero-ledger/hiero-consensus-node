# SPDX-License-Identifier: Apache-2.0
name: "ZXC: Execute HAPI Tests"
on:
  workflow_call:
    inputs:
      ref:
        description: "The branch, tag or SHA to checkout."
        required: false
        type: string
        default: ""
      java-distribution:
        description: "Java JDK Distribution:"
        type: string
        required: false
        default: "temurin"
      java-version:
        description: "Java JDK Version:"
        type: string
        required: false
        default: "25"
      node-version:
        description: "NodeJS Version:"
        type: string
        required: false
        default: "20"
      enable-network-log-capture:
        description: "Network Log Capture Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-misc-and-nd-reconnect:
        description: "HAPI Testing (Misc & ND Reconnect) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-misc-records-and-crypto:
        description: "HAPI Testing (Misc Records & Crypto) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-token-and-time-consuming:
        description: "HAPI Testing (Token & Time Consuming) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-atomic-batch-and-iss:
        description: "HAPI Testing (Atomic Batch & ISS) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-simplefees:
        description: "HAPI Testing (simplefees) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-smart-contract:
        description: "HAPI Testing (smart contract) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-restart:
        description: "HAPI Testing (restart) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-bn-communication:
        description: "HAPI Testing (Block Node Communication) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-state-throttling:
        description: "HAPI Testing (state throttling) Enabled"
        type: string
        required: false
        default: "false"
    secrets:
      access-token:
        description: "The GitHub access token used to checkout the repository, submodules, and make GitHub API calls."
        required: true
      gradle-cache-username:
        description: "The username used to authenticate with the Gradle Build Cache Node."
        required: true
      gradle-cache-password:
        description: "The password used to authenticate with the Gradle Build Cache Node."
        required: true
    outputs:
      failure-mode:
        description: "Failure Mode"
        value: ${{ jobs.set-failure-mode.outputs.failure-mode }}

defaults:
  run:
    shell: bash

permissions:
  id-token: write
  actions: read
  pull-requests: write
  statuses: write
  checks: write
  contents: read

env:
  GRADLE_CACHE_USERNAME: ${{ secrets.gradle-cache-username }}
  GRADLE_CACHE_PASSWORD: ${{ secrets.gradle-cache-password }}
  GRADLE_EXEC: ionice -c 2 -n 2 nice -n 19 ./gradlew
  CG_EXEC: ionice -c 2 -n 2 nice -n 19

jobs:
  hapi-tests-misc-and-nd-reconnect:
    name: "HAPI Tests (Misc & ND Reconnect)"
    if: ${{ inputs.enable-hapi-tests-misc-and-nd-reconnect == 'true' }}
    runs-on: hl-cn-hapi-lin-xl
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Misc & ND Reconnect)
        id: gradle-hapi-misc-and-nd-reconnect
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        # Run each task in isolation with clean builds because we dynamically reconfigure Log4j for each mode
        run: ${GRADLE_EXEC} hapiTestMisc --no-daemon && ${GRADLE_EXEC} hapiTestMiscEmbedded --no-daemon && ${GRADLE_EXEC} hapiTestMiscRepeatable --no-daemon && ${GRADLE_EXEC} hapiTestMiscSerial --no-daemon && ${GRADLE_EXEC} hapiTestNDReconnect --no-daemon

      - name: Publish HAPI Test (Misc & ND Reconnect) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Misc & ND Reconnect) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/**/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Misc & ND Reconnect) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Misc & ND Reconnect) Reports
          path: "**/test-clients/build/test-results/**/TEST-*.xml"
          retention-days: 7

      - name: Detect Flaky Tests (Misc & ND Reconnect)
        id: detect-flaky-hapi-misc-and-nd-reconnect
        if: ${{ !cancelled() }}
        run: |
          has_flaky="false"
          if grep -rql "<flakyFailure" hedera-node/test-clients/build/test-results/ --include="TEST-*.xml" 2>/dev/null; then
            has_flaky="true"
          fi
          echo "has-flaky=${has_flaky}" >> "${GITHUB_OUTPUT}"

      - name: Upload HAPI Test (Misc & ND Reconnect) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && inputs.enable-network-log-capture && (steps.gradle-hapi-misc-and-nd-reconnect.conclusion == 'failure' || steps.detect-flaky-hapi-misc-and-nd-reconnect.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Misc & ND Reconnect) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Misc & ND Reconnect) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && (steps.gradle-hapi-misc-and-nd-reconnect.conclusion == 'failure' || steps.detect-flaky-hapi-misc-and-nd-reconnect.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Misc & ND Reconnect) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/**/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

  hapi-tests-misc-records-and-crypto:
    name: "HAPI Tests (Misc Records & Crypto)"
    if: ${{ inputs.enable-hapi-tests-misc-records-and-crypto == 'true' }}
    runs-on: hl-cn-hapi-lin-xl
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Misc Records & Crypto)
        id: gradle-hapi-misc-records-and-crypto
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestMiscRecords --no-daemon && ${GRADLE_EXEC} hapiTestMiscRecordsSerial --no-daemon && ${GRADLE_EXEC} hapiTestCrypto --no-daemon && ${GRADLE_EXEC} hapiTestCryptoEmbedded --no-daemon && ${GRADLE_EXEC} hapiTestCryptoSerial --no-daemon

      - name: Publish HAPI Test (Misc Records & Crypto) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Misc Records & Crypto) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/**/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Misc Records & Crypto) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Misc Records & Crypto) Reports
          path: "**/test-clients/build/test-results/**/TEST-*.xml"
          retention-days: 7

      - name: Detect Flaky Tests (Misc Records & Crypto)
        id: detect-flaky-hapi-misc-records-and-crypto
        if: ${{ !cancelled() }}
        run: |
          has_flaky="false"
          if grep -rql "<flakyFailure" hedera-node/test-clients/build/test-results/ --include="TEST-*.xml" 2>/dev/null; then
            has_flaky="true"
          fi
          echo "has-flaky=${has_flaky}" >> "${GITHUB_OUTPUT}"

      - name: Upload HAPI Test (Misc Records & Crypto) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && inputs.enable-network-log-capture && (steps.gradle-hapi-misc-records-and-crypto.conclusion == 'failure' || steps.detect-flaky-hapi-misc-records-and-crypto.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Misc Records & Crypto) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Misc Records & Crypto) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && (steps.gradle-hapi-misc-records-and-crypto.conclusion == 'failure' || steps.detect-flaky-hapi-misc-records-and-crypto.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Misc Records & Crypto) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/**/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

  hapi-tests-simple-fees:
    name: "HAPI Tests (Simple Fees)"
    if: ${{ inputs.enable-hapi-tests-simplefees == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Simple Fees)
        id: gradle-hapi-simplefees
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestSimpleFees --no-daemon && ${GRADLE_EXEC} hapiTestSimpleFeesEmbedded --no-daemon && ${GRADLE_EXEC} hapiTestSimpleFeesSerial --no-daemon

      - name: Publish HAPI Test (Simple Fees) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Simple Fees) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/**/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Simple Fees) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Simple Fees) Report
          path: "**/test-clients/build/test-results/**/TEST-*.xml"
          retention-days: 7

      - name: Detect Flaky Tests (Simple Fees)
        id: detect-flaky-hapi-simplefees
        if: ${{ !cancelled() }}
        run: |
          has_flaky="false"
          if grep -rql "<flakyFailure" hedera-node/test-clients/build/test-results/ --include="TEST-*.xml" 2>/dev/null; then
            has_flaky="true"
          fi
          echo "has-flaky=${has_flaky}" >> "${GITHUB_OUTPUT}"

      - name: Upload HAPI Test (Simple Fees) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && inputs.enable-network-log-capture && (steps.gradle-hapi-simplefees.conclusion == 'failure' || steps.detect-flaky-hapi-simplefees.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Simple Fees) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Simple Fees) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && (steps.gradle-hapi-simplefees.conclusion == 'failure' || steps.detect-flaky-hapi-simplefees.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Simple Fees) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/**/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

  hapi-tests-token-and-time-consuming:
    name: "HAPI Tests (Token & Time Consuming)"
    if: ${{ inputs.enable-hapi-tests-token-and-time-consuming == 'true' }}
    runs-on: hl-cn-hapi-lin-xl
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Token & Time Consuming)
        id: gradle-hapi-token-and-time-consuming
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestToken --no-daemon && ${GRADLE_EXEC} hapiTestTokenSerial --no-daemon && ${GRADLE_EXEC} hapiTestTimeConsuming --no-daemon && ${GRADLE_EXEC} hapiTestTimeConsumingSerial --no-daemon

      - name: Publish HAPI Test (Token & Time Consuming) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Token & Time Consuming) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/**/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Token & Time Consuming) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Token & Time Consuming) Reports
          path: "**/test-clients/build/test-results/**/TEST-*.xml"
          retention-days: 7

      - name: Detect Flaky Tests (Token & Time Consuming)
        id: detect-flaky-hapi-token-and-time-consuming
        if: ${{ !cancelled() }}
        run: |
          has_flaky="false"
          if grep -rql "<flakyFailure" hedera-node/test-clients/build/test-results/ --include="TEST-*.xml" 2>/dev/null; then
            has_flaky="true"
          fi
          echo "has-flaky=${has_flaky}" >> "${GITHUB_OUTPUT}"

      - name: Upload HAPI Test (Token & Time Consuming) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && inputs.enable-network-log-capture && (steps.gradle-hapi-token-and-time-consuming.conclusion == 'failure' || steps.detect-flaky-hapi-token-and-time-consuming.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Token & Time Consuming) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Token & Time Consuming) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && (steps.gradle-hapi-token-and-time-consuming.conclusion == 'failure' || steps.detect-flaky-hapi-token-and-time-consuming.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Token & Time Consuming) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/**/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

  hapi-tests-smart-contracts:
    name: "HAPI Tests (Smart Contracts)"
    if: ${{ inputs.enable-hapi-tests-smart-contract == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Smart Contract)
        id: gradle-hapi-smart-contract
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestSmartContract --no-daemon && ${GRADLE_EXEC} hapiTestSmartContractSerial --no-daemon

      - name: Publish HAPI Test (Smart Contract) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Smart Contract) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/**/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Smart Contract) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Smart Contract) Report
          path: "**/test-clients/build/test-results/**/TEST-*.xml"
          retention-days: 7

      - name: Detect Flaky Tests (Smart Contract)
        id: detect-flaky-hapi-smart-contract
        if: ${{ !cancelled() }}
        run: |
          has_flaky="false"
          if grep -rql "<flakyFailure" hedera-node/test-clients/build/test-results/ --include="TEST-*.xml" 2>/dev/null; then
            has_flaky="true"
          fi
          echo "has-flaky=${has_flaky}" >> "${GITHUB_OUTPUT}"

      - name: Upload HAPI Test (Smart Contract) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && inputs.enable-network-log-capture && (steps.gradle-hapi-smart-contract.conclusion == 'failure' || steps.detect-flaky-hapi-smart-contract.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Smart Contract) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Smart Contract) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && (steps.gradle-hapi-smart-contract.conclusion == 'failure' || steps.detect-flaky-hapi-smart-contract.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Smart Contract) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/**/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

  hapi-tests-restart:
    name: "HAPI Tests (Restart)"
    if: ${{ inputs.enable-hapi-tests-restart == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Restart)
        id: gradle-hapi-restart
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestRestart

      - name: Publish HAPI Test (Restart) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Restart) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Restart) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Restart) Report
          path: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          retention-days: 7

      - name: Detect Flaky Tests (Restart)
        id: detect-flaky-hapi-restart
        if: ${{ !cancelled() }}
        run: |
          has_flaky="false"
          if grep -rql "<flakyFailure" hedera-node/test-clients/build/test-results/ --include="TEST-*.xml" 2>/dev/null; then
            has_flaky="true"
          fi
          echo "has-flaky=${has_flaky}" >> "${GITHUB_OUTPUT}"

      - name: Upload HAPI Test (Restart) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && inputs.enable-network-log-capture && (steps.gradle-hapi-restart.conclusion == 'failure' || steps.detect-flaky-hapi-restart.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Restart) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Restart) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && (steps.gradle-hapi-restart.conclusion == 'failure' || steps.detect-flaky-hapi-restart.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Restart) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/**/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

  hapi-tests-bn-comms:
    name: "HAPI Tests (BN Comms)"
    if: ${{ inputs.enable-hapi-tests-bn-communication == 'true' }}
    runs-on: hl-cn-hapi-bn-lin-xl
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Block Node Communication)
        id: gradle-hapi-bn-communication
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestBlockNodeCommunication

      - name: Publish HAPI Test (Block Node Communication) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Block Node Communication) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Block Node Communication) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Block Node Communication) Report
          path: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          retention-days: 7

      - name: Detect Flaky Tests (Block Node Communication)
        id: detect-flaky-hapi-bn-communication
        if: ${{ !cancelled() }}
        run: |
          has_flaky="false"
          if grep -rql "<flakyFailure" hedera-node/test-clients/build/test-results/ --include="TEST-*.xml" 2>/dev/null; then
            has_flaky="true"
          fi
          echo "has-flaky=${has_flaky}" >> "${GITHUB_OUTPUT}"

      - name: Upload HAPI Test (Block Node Communication) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && inputs.enable-network-log-capture && (steps.gradle-hapi-bn-communication.conclusion == 'failure' || steps.detect-flaky-hapi-bn-communication.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Block Node Communication) Network Logs
          path: |
            hedera-node/test-clients/build/*-test/**/output/**
          retention-days: 7

      - name: Upload HAPI Test (Block Node Communication) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && (steps.gradle-hapi-bn-communication.conclusion == 'failure' || steps.detect-flaky-hapi-bn-communication.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Block Node Communication) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
            hedera-node/test-clients/build/*-test/**/output/**
            hedera-node/test-clients/build/*-test/**/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

  hapi-tests-atomic-batch-and-iss:
    name: "HAPI Tests (Atomic Batch & ISS)"
    if: ${{ inputs.enable-hapi-tests-atomic-batch-and-iss == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Atomic Batch & ISS)
        id: gradle-hapi-atomic-batch-and-iss
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestAtomicBatch --no-daemon && ${GRADLE_EXEC} hapiTestAtomicBatchSerial --no-daemon && ${GRADLE_EXEC} hapiTestAtomicBatchEmbedded --no-daemon && ${GRADLE_EXEC} hapiTestIss --no-daemon

      - name: Publish HAPI Test (Atomic Batch & ISS) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Atomic Batch & ISS) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/**/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Atomic Batch & ISS) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Atomic Batch & ISS) Reports
          path: "**/test-clients/build/test-results/**/TEST-*.xml"
          retention-days: 7

      - name: Detect Flaky Tests (Atomic Batch & ISS)
        id: detect-flaky-hapi-atomic-batch-and-iss
        if: ${{ !cancelled() }}
        run: |
          has_flaky="false"
          if grep -rql "<flakyFailure" hedera-node/test-clients/build/test-results/ --include="TEST-*.xml" 2>/dev/null; then
            has_flaky="true"
          fi
          echo "has-flaky=${has_flaky}" >> "${GITHUB_OUTPUT}"

      - name: Upload HAPI Test (Atomic Batch & ISS) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && inputs.enable-network-log-capture && (steps.gradle-hapi-atomic-batch-and-iss.conclusion == 'failure' || steps.detect-flaky-hapi-atomic-batch-and-iss.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Atomic Batch & ISS) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Atomic Batch & ISS) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && (steps.gradle-hapi-atomic-batch-and-iss.conclusion == 'failure' || steps.detect-flaky-hapi-atomic-batch-and-iss.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (Atomic Batch & ISS) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/**/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

  hapi-tests-state-throttling:
    name: "HAPI Tests (State Throttling)"
    if: ${{ inputs.enable-hapi-tests-state-throttling == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (State Throttling)
        id: gradle-hapi-state-throttling
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestStateThrottling

      - name: Publish HAPI Test (State Throttling) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (State Throttling) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (State Throttling) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (State Throttling) Report
          path: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          retention-days: 7

      - name: Detect Flaky Tests (State Throttling)
        id: detect-flaky-hapi-state-throttling
        if: ${{ !cancelled() }}
        run: |
          has_flaky="false"
          if grep -rql "<flakyFailure" hedera-node/test-clients/build/test-results/ --include="TEST-*.xml" 2>/dev/null; then
            has_flaky="true"
          fi
          echo "has-flaky=${has_flaky}" >> "${GITHUB_OUTPUT}"

      - name: Upload HAPI Test (State Throttling) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && inputs.enable-network-log-capture && (steps.gradle-hapi-state-throttling.conclusion == 'failure' || steps.detect-flaky-hapi-state-throttling.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (State Throttling) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (State Throttling) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ !cancelled() && (steps.gradle-hapi-state-throttling.conclusion == 'failure' || steps.detect-flaky-hapi-state-throttling.outputs.has-flaky == 'true') }}
        with:
          name: HAPI Test (State Throttling) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/**/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

  set-failure-mode:
    name: "Set Failure Mode"
    runs-on: hl-cn-default-lin-sm
    needs:
      - hapi-tests-misc-and-nd-reconnect
      - hapi-tests-misc-records-and-crypto
      - hapi-tests-simple-fees
      - hapi-tests-token-and-time-consuming
      - hapi-tests-smart-contracts
      - hapi-tests-restart
      - hapi-tests-bn-comms
      - hapi-tests-atomic-batch-and-iss
      - hapi-tests-state-throttling
    if: ${{ !cancelled() && always() }} # always run unless cancelled
    outputs:
      failure-mode: ${{ steps.set-failure-mode.outputs.failure-mode }}
    steps:
      - name: Initialize GitHub Job
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1

      - name: Set Failure Mode
        id: set-failure-mode
        run: |
          job_statuses=(
            "${{ needs.hapi-tests-misc.result || 'skipped' }}"
            "${{ needs.hapi-tests-misc-records.result || 'skipped' }}"
            "${{ needs.hapi-tests-crypto.result || 'skipped' }}"
            "${{ needs.hapi-tests-simple-fees.result || 'skipped' }}"
            "${{ needs.hapi-tests-token.result || 'skipped' }}"
            "${{ needs.hapi-tests-smart-contracts.result || 'skipped' }}"
            "${{ needs.hapi-tests-time-consuming.result || 'skipped' }}"
            "${{ needs.hapi-tests-restart.result || 'skipped' }}"
            "${{ needs.hapi-tests-nd-reconnect.result || 'skipped' }}"
            "${{ needs.hapi-tests-iss.result || 'skipped' }}"
            "${{ needs.hapi-tests-bn-comms.result || 'skipped' }}"
            "${{ needs.hapi-tests-atomic-batch.result || 'skipped' }}"
            "${{ needs.hapi-tests-state-throttling.result || 'skipped' }}"
          )

          # final outputs
          failure_mode="none"

          has_failure() {
            local arr=("$@")
            for status in "${arr[@]}"; do
              [[ "${status}" == "failure" ]] && return 0
            done
            return 1
          }

          if has_failure "${job_statuses[@]}"; then
            failure_mode="general"
          fi

          echo "failure-mode=${failure_mode}" >> "${GITHUB_OUTPUT}"
